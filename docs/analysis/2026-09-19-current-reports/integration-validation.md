# 병합·화면·산출물 검증 · 2026-09-19

## 병합과 변경 범위

- 작업 브랜치: `codex/c2-structural-integrity-20260916`
- 병합 대상: `codex/c2-flow-role-context-20260917`
- `8ccca548391d3ffe1f5db489803a20a688ed5ad0`에서
  `92dc503f43c13852a42a6c485948b28c1da544e2`로 fast-forward 병합했다.
- 이번 추가 변경은 기록 패널의 접기/펼치기, 활성 경로 표시, BDA 시각의 결과 표시,
  보고서·파라미터 내보내기와 문서이다. 시뮬레이션 엔진은 변경하지 않았다.

## 화면 검증

`prototype/command-flow.html`을 수정하고 `scripts/build-command-flow-single.mjs`로
루트의 지휘흐름 단일본을 다시 생성했다.

실제 Chrome에서 생성된 단일 HTML을 열고 시뮬레이션을 실행한 뒤 항적 개별 탭에서 확인했다.

- 기본 상태는 기록 펼침이다. 제목을 클릭하면 제어·기록 영역이 접히고 제목은 남는다.
- 접으면 해당 시험 화면의 그래프 높이가 289px에서 520.5px로 늘어난다.
- 제목에 키보드 초점을 둔 상태에서 Enter로 다시 펼칠 수 있다.
- 접기/펼치기 전후 선택 항적과 기록 내용이 유지된다.
- 전체 탭에 갔다가 항적 개별 탭으로 돌아와도 접힘 상태가 유지된다.
- 브라우저 JavaScript 오류는 0개였다.

현재 변경에 대해 통과한 저장소 검사:

```bash
node tests/command-flow-config.test.mjs
node tests/command-flow-single.test.mjs
node tests/prototype-readability.test.mjs
node scripts/build-command-flow-single.mjs --check
node docs/analysis/2026-09-19-current-reports/c2-analysis-run.mjs --self-test
```

설정 테스트의 6개 검사에는 활성 경로의 강조 보존 및 BDA 직전·정확한 시각·이후의
HIT/MISS 표시 검사가 포함된다. 2026-09-16의 전체 52개 테스트 통과는 이전 구조 수정의
검증 기록이며, 이번 변경에서 전체 52개를 다시 실행했다는 의미가 아니다.

## 분석 자료 교차 검산

- 일반 분석 182개 셀과 별도 탐침 1개를 현재 소스에서 실행했다.
- C2 기록 실행 4개를 별도로 수행했다. 기록 절삭 없이 206개 최초 발사 항적의 시간을 연결했다.
- 두 JSON의 엔진·카탈로그 어댑터·프로토타입 SHA-256이 서로 일치하고 현재 파일과도 일치한다.
- 네 구성에서 생성·격추·누수·미해결·발사 횟수·발사 항적 수가 일반/C2 분석 사이에 일치한다.
- 일반 분석의 탐지→최초 실제 발사 평균과 C2 계보 재구성 평균이 일치한다.
- C2 다섯 구간의 평균 합계는 동일 표본의 총평균과 일치한다.
- 두 배치 각각 20개 쌍대 seed에 대해 8개 지표의 평균·표본 표준편차·Student t(19)
  95% 신뢰구간을 별도 Python 계산으로 검산했다.
- 종료 미해결을 격추·누수에 임의 배분하지 않았고, 관측 역할 설명을 실제 통신으로 세지 않았다.

## 파라미터 파일

기존 13개 시트의 순서·기본 서식·창 고정을 유지했다. 원본 대비 값 896셀을 갱신하고,
내보낸 25,133셀을 소스에서 추출한 값과 대조했다. 수식은 변경 전후 모두 0개이며 오류는
0개다. 시트별 검산 결과는 [xlsx-qa.json](xlsx-qa.json)에 남겼다. 모든 시트를 렌더링해
확인했다. XLSX는 참조용 내보내기이며 엔진 입력 파일이 아니다.

PDF의 페이지 수·레이아웃 자동 검사 결과와 전 페이지 시각 검토는 이 폴더의 렌더링 QA
기록에 별도로 남긴다.

## 추가 · 보고서 통합 (같은 날, 소스 `d398215`)

위 기록은 두 PDF를 따로 내던 시점의 것이다. 이후 두 문서를 SC3 기준 한 권으로 합쳤다.

- 화면 표시 변경(`d398215`: 지도 항적 이름, 상단 설명 영역 삭제) 뒤 두 분석을 다시 실행했다.
  엔진·어댑터 해시는 같고 프로토타입 해시만 바뀌었으며, 두 JSON의 `meta`를 뺀 본문은 직전과 같다.
- 위 「분석 자료 교차 검산」의 일치 항목(생성·격추·누수·미해결·발사 횟수, 시간 측정 항적 수,
  탐지→최초 발사 평균)은 이제 `report-gen.mjs`가 매번 어서션으로 검사한다.
- `K-JAMDS_C2_분석결과.pdf`, `c2-report-gen.mjs`, `c2-report.html`은 삭제했다. 이전 판은 Git 이력에 있다.
- 쪽수·레이아웃 검사와 전 페이지 시각 검토는 `QA.md`에 적는다.
